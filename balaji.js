import React from 'react'
const Balaji=(props)=>{ 
    return(
        

        <React.Fragment> 
            <h1>My name is {props.name} im from {props.children}</h1>
        </React.Fragment>

    );

}
export default Balaji;
